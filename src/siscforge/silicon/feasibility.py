"""Silicon Feasibility scorer (transparent heuristics).

RESTORE IN PROGRESS — full content follows in next commit if this is truncated.
"""
from __future__ import annotations
raise ImportError("feasibility.py restore incomplete — see PR #8")
